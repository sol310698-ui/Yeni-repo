<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.WearCommands" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/brand_dark</item>
        <item name="colorOnPrimary">@color/on_brand_dark</item>
        <item name="colorPrimaryContainer">@color/brand_container_dark</item>
        <item name="colorOnPrimaryContainer">@color/on_brand_container_dark</item>
        <item name="android:windowBackground">@drawable/bg_app</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Yakalama Activity'si: opak siyah (kamera daha kararli calisir) -->
    <style name="Theme.Capture" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
